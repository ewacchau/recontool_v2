# --- Core secrets ---
# IMPORTANT: Set a strong secret key in production
SECRET_KEY=replace-me

# --- Database ---
# Docker (Postgres in docker-compose):
# DATABASE_URL=postgresql+psycopg2://recon:reconpass@db:5432/recon

# Local (SQLite default if unset):
# DATABASE_URL=sqlite:///recon.db

# --- Celery / Redis ---
# Docker defaults point to the docker-compose service name "redis".
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Flask-Limiter storage (use Redis for shared, durable limits)
RATELIMIT_STORAGE_URI=redis://redis:6379/1

# --- Optional APIs ---
# SHODAN_API_KEY=

# --- Config class selection ---
# Use DevelopmentConfig by default. For production uncomment the next line.
# APP_CONFIG=app.config.ProductionConfig

# --- Cookie / security overrides (ProductionConfig sets these to secure) ---
# SESSION_COOKIE_SAMESITE=Lax
# SESSION_COOKIE_SECURE=false
# REMEMBER_COOKIE_SECURE=false
# PREFERRED_URL_SCHEME=http

